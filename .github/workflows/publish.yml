name: Publish to Maven Central

on:
  release:
    types: [released]
  workflow_dispatch:
    inputs:
      publish:
        description: "Publish and release to Maven Central (requires secrets)"
        type: boolean
        required: false
        default: false

jobs:
  publish:
    name: Publish to Maven Central
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '21'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Prepare ast_distance parity inputs
        run: |
          rm -rf tmp/lalrpop-rs
          git clone --depth 1 --branch 0.23.1 https://github.com/lalrpop/lalrpop.git tmp/lalrpop-rs
          cmake -S tools/ast_distance -B tools/ast_distance/build
          cmake --build tools/ast_distance/build --parallel
          cp tools/ast_distance/build/ast_distance tools/ast_distance/ast_distance

      - name: Build (dry run)
        if: ${{ github.event_name == 'workflow_dispatch' && !inputs.publish }}
        run: ./gradlew compileKotlinMacosArm64 --no-configuration-cache

      - name: Publish to Maven Central
        if: ${{ github.event_name == 'release' || (github.event_name == 'workflow_dispatch' && inputs.publish) }}
        run: ./gradlew publishAndReleaseToMavenCentral --no-configuration-cache
        env:
          ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.ORG_GRADLE_PROJECT_mavenCentralUsername }}
          ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.ORG_GRADLE_PROJECT_mavenCentralPassword }}
          ORG_GRADLE_PROJECT_signingInMemoryKeyId: ${{ secrets.ORG_GRADLE_PROJECT_signingInMemoryKeyId }}
          ORG_GRADLE_PROJECT_signingInMemoryKeyPassword: ${{ secrets.ORG_GRADLE_PROJECT_signingInMemoryKeyPassword }}
          ORG_GRADLE_PROJECT_signingInMemoryKey: ${{ secrets.ORG_GRADLE_PROJECT_signingInMemoryKey }}
